//Default component that is called inside a custom folder 
//Runs whatever thing when we only call the folder's name on the route link

const ProfileDefault=()=>{
    return(
        <h1>Profile page</h1>
    )
}
export default ProfileDefault;